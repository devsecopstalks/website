[project]
name = "podcast-tools"
version = "0.1.0"
description = "Automated podcast publishing tools"
requires-python = ">=3.13"
dependencies = [
    "requests",
    "jinja2",
    "upload-post",
    "openai",
    "httpx",
    "markdown",
]
