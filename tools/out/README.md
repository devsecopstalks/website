# Pipeline output

Transcripts, drafts, reviews, and progress markers for the podcast publish pipeline are written here as `"{audio-stem}-*.md"` and related files. This directory is gitignored except this README.

Drop source audio under `../raw/` and run `./do.sh` or `uv run python podbean.py …`.
